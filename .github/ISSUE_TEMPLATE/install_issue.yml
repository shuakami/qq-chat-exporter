name: "🔧 Installation Issue / 安装问题"
description: "Can't get QCE running? / QCE 跑不起来？"
title: "[Install] "
labels: ["bug", "installation"]
body:
  - type: markdown
    attributes:
      value: |
        ## 🔧 Installation Help / 安装帮助
        
        Having trouble setting up QCE? We'll help!
        安装 QCE 遇到问题？我们来帮忙！
        
        > 📖 Check the [README](https://github.com/shuakami/qq-chat-exporter#readme) first!
        > 先看看 README！

  - type: dropdown
    id: os
    attributes:
      label: "💻 System / 系统"
      options:
        - Windows 11
        - Windows 10
        - Ubuntu / Debian
        - Other Linux
        - Docker
    validations:
      required: true

  - type: dropdown
    id: install-method
    attributes:
      label: "📥 Install Method / 安装方式"
      options:
        - "Official Release (一键启动包)"
        - "Manual Install (手动安装)"
        - "Docker"
        - "Installing to existing NapCat / 安装到已有 NapCat"
    validations:
      required: true

  - type: dropdown
    id: stage
    attributes:
      label: "📍 Where did it fail? / 在哪一步失败？"
      options:
        - "Download / 下载"
        - "Extracting / 解压"
        - "Starting QCE / 启动 QCE"
        - "QQ Login / QQ 登录"
        - "Opening Web UI / 打开 Web 界面"
        - "Other / 其他"
    validations:
      required: true

  - type: textarea
    id: error
    attributes:
      label: "📋 Error message / 错误信息"
      render: shell
      placeholder: |
        Paste the error from terminal
        粘贴终端的错误信息
        
        [ERROR] xxx
    validations:
      required: true

  - type: input
    id: qq-path
    attributes:
      label: "📂 QQ Install Path / QQ 安装路径"
      description: "If using custom path / 如果使用自定义路径"
      placeholder: "D:\\Program Files\\Tencent\\QQNT"

  - type: textarea
    id: tried
    attributes:
      label: "🔍 What have you tried? / 尝试过什么？"
      placeholder: |
        - Ran as admin / 以管理员运行
        - Reinstalled QQ / 重装 QQ
        - ...

  - type: textarea
    id: screenshots
    attributes:
      label: "📸 Screenshots / 截图"
      description: "Drag & drop images / 拖拽图片"
